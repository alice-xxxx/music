import QtQuick
import QtQuick.Controls as Controls
import "../theme"

Controls.ToolBar {
    implicitHeight: 60
    leftPadding: 8
    rightPadding: 8
    background: Rectangle { color: Theme.background }
}
